import { PageHero } from '@/components/sections/page-hero';

export function LibraryHero() {
  return (
    <PageHero
      description="Browse migrated hero-section concepts rebuilt for a production Next.js, TypeScript, and Tailwind CSS system. Each pattern keeps the original intent while improving hierarchy, responsiveness, and accessibility."
      eyebrow="Component library"
      primaryHref="/contact"
      primaryLabel="Request a custom section"
      secondaryHref="#examples"
      secondaryLabel="View examples"
      title="Hero sections that feel ready for a real launch."
    />
  );
}
